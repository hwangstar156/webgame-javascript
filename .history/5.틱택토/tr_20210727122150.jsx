const React = require('react');
const Td = require('./td');

const Tr = (rowData) => {
    console.dir(rowData);
    return (
        <tr>
            <Td>{''}</Td>
        </tr>
    );
};

module.exports = Tr;